# Screenshots + Diagrams (ES)

Captured from the local ActiveAdmin interface in Spanish. Replace or update as needed,
but keep filenames stable so the guides stay in sync.

Mermaid diagrams are stored as `.mmd` with rendered `.png`/`.svg` counterparts.
