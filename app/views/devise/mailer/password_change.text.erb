<% @preheader_text = t("devise.mailer.password_change.preheader", app_name: app_name) %>
<%= render "shared/mailer/message",
  formats: [:text],
  tone: :info,
  category: t("mailers.shared.categories.account_security"),
  title: t("devise.mailer.password_change.title"),
  greeting: t("devise.mailer.password_change.greeting", email: @resource.email),
  intro: t("devise.mailer.password_change.notice") %>
