<% @preheader_text = t("billing_mailer.subscription_started.preheader", app_name: app_name) %>
<%= render "shared/mailer/message",
  formats: [:text],
  tone: :success,
  category: t("mailers.shared.categories.billing"),
  title: t("billing_mailer.subscription_started.title"),
  greeting: t("billing_mailer.greeting", name: recipient_name),
  intro: t("billing_mailer.subscription_started.intro", app_name: app_name),
  detail_rows: subscription_detail_rows,
  primary_action: {
    label: t(@discord_activation_url.present? ? "billing_mailer.subscription_started.discord_cta" : "billing_mailer.subscription_started.billing_cta"),
    url: @discord_activation_url.presence || @billing_url
  },
  secondary_action: @invoice_url.present? ? {
    label: t("billing_mailer.invoice_cta"),
    url: @invoice_url
  } : nil %>
