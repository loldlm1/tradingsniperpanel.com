<% content_for :title, "#{t('devise.sign_up_title')} | #{app_name}" %>
<% content_for :full_bleed, true %>

<main class="font-inter antialiased bg-gray-900 text-gray-100 tracking-tight">
  <div class="flex flex-col min-h-screen overflow-hidden">
    <header class="absolute w-full z-30">
      <div class="max-w-6xl mx-auto px-4 sm:px-6">
        <div class="flex items-center justify-between h-16 md:h-20">
          <div class="shrink-0 mr-4">
            <%= link_to root_path, class: "block", "aria-label": app_name do %>
              <%= render "shared/marketing_logo", logo_class: "w-12 h-12 object-contain" %>
            <% end %>
          </div>
        </div>
      </div>
    </header>

    <div class="relative grow flex">
      <div class="w-full bg-gray-900">
        <div class="h-full flex flex-col justify-center before:min-h-[4rem] md:before:min-h-[5rem] before:flex-1 after:flex-1">
          <div class="px-4 sm:px-6">
            <div class="w-full max-w-sm mx-auto">
              <div class="py-16 md:py-20">
                <div class="mb-8">
                  <h1 class="h2 font-uncut-sans"><%= t("devise.sign_up_title") %></h1>
                </div>

                <%= render "devise/shared/flash" %>

                <%= form_with model: resource, scope: resource_name, url: registration_path(resource_name), class: "space-y-4" do |f| %>
                  <%= render "devise/shared/error_messages", resource: resource %>
                  <div>
                    <%= f.label :email, class: "block text-sm text-gray-400 font-medium mb-1" %>
                    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-input py-2 w-full" %>
                  </div>
                  <div>
                    <%= f.label :name, class: "block text-sm text-gray-400 font-medium mb-1" %>
                    <%= f.text_field :name, autocomplete: "name", class: "form-input py-2 w-full" %>
                  </div>
                  <div>
                    <%= f.label :password, class: "block text-sm text-gray-400 font-medium mb-1" %>
                    <%= f.password_field :password, autocomplete: "new-password", class: "form-input py-2 w-full" %>
                  </div>
                  <div>
                    <%= f.label :password_confirmation, class: "block text-sm text-gray-400 font-medium mb-1" %>
                    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-input py-2 w-full", required: true, "aria-required": true %>
                  </div>
                  <div class="p-4 rounded-lg border border-gray-800 bg-gray-950/40">
                    <label class="flex items-center">
                      <%= f.check_box :terms_of_service, { required: true, class: "form-checkbox text-blue-500" }, "1", "0" %>
                      <span class="text-sm ml-2 text-gray-300">
                        <%= t("devise.terms_checkbox_html", terms_link: link_to(t("legal.links.terms"), terms_path, class: "text-blue-400 underline"), privacy_link: link_to(t("legal.links.privacy"), privacy_path, class: "text-blue-400 underline")).html_safe %>
                      </span>
                    </label>
                  </div>
                  <div class="mt-6">
                    <%= f.submit t("devise.sign_up_title"), class: "btn-sm text-white bg-linear-to-t from-blue-600 to-blue-400 hover:to-blue-500 w-full shadow-lg group flex items-center justify-center gap-2" %>
                  </div>
                <% end %>

                <div class="flex items-center my-6">
                  <div class="border-t border-gray-800 grow mr-3" aria-hidden="true"></div>
                  <div class="text-sm text-gray-400 italic"><%= t("devise.or") %></div>
                  <div class="border-t border-gray-800 grow ml-3" aria-hidden="true"></div>
                </div>

                <%= button_to user_google_oauth2_omniauth_authorize_path(locale: I18n.locale),
                              method: :post,
                              class: "btn-sm text-white bg-linear-to-t from-pink-500 to-pink-400 hover:to-pink-500 w-full relative flex after:flex-1" do %>
                  <div class="flex-1 flex items-center">
                    <svg class="w-4 h-4 fill-current text-pink-200 shrink-0" viewBox="0 0 16 16" xmlns="http://www.w3.org/2000/svg">
                      <path d="M15.679 6.545H8.043v3.273h4.328c-.692 2.182-2.401 2.91-4.363 2.91a4.727 4.727 0 1 1 3.035-8.347l2.378-2.265A8 8 0 1 0 8.008 16c4.41 0 8.4-2.909 7.67-9.455Z" />
                    </svg>
                  </div>
                  <span class="flex-auto text-pink-50 pl-3"><%= t("devise.continue_with_google") %></span>
                <% end %>

                <div class="text-sm text-gray-500 italic mt-6 mb-4">
                  <%= t("devise.consent_copy") %>
                </div>

                <div class="pt-4 border-t border-gray-800">
                  <div class="text-sm text-gray-400">
                    <%= t("devise.have_account") %>
                    <%= link_to t("nav.sign_in"), new_session_path(resource_name), class: "font-medium text-blue-500 hover:text-blue-400" %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="hidden lg:block shrink-0 w-1/3 overflow-hidden before:bg-gray-800 before:absolute before:inset-0 before:-z-10">
        <div class="absolute left-1/2 -translate-x-1/2 pointer-events-none -z-10" aria-hidden="true">
          <%= image_tag "neon/images/hero-illustration.svg", class: "max-w-none", width: 2143, height: 737, alt: t("devise.hero_illustration_alt") %>
        </div>
        <div class="absolute left-1/2 -translate-x-1/2 pointer-events-none -z-10" aria-hidden="true">
          <%= image_tag "neon/images/auth-illustration.svg", class: "max-w-none", width: 1440, height: 880, alt: t("devise.auth_illustration_alt") %>
        </div>
      </div>
    </div>
  </div>
</main>
