<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <meta charset="utf-8">
    <title><%= content_for(:title) || "#{t('app.name')} Dashboard" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "mosaic/style", "data-turbo-track": "reload" %>
    <%= yield :head %>
  </head>
  <body class="<%= content_for?(:body_class) ? yield(:body_class) : "font-inter antialiased bg-slate-100 text-slate-900 dark:bg-slate-900 dark:text-slate-100" %>">
    <div class="flex min-h-screen bg-slate-100 dark:bg-slate-900">
      <div class="flex-1 flex flex-col">
        <header class="sticky top-0 z-40 bg-white/80 dark:bg-slate-900/80 backdrop-blur border-b border-slate-200 dark:border-slate-800">
          <div class="max-w-6xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between">
            <div class="flex items-center gap-3">
              <%= link_to root_path, class: "flex items-center gap-2" do %>
                <img src="/docs/sniper_advanced_panel/White_SAP_logo.png" alt="<%= t("app.name") %>" class="h-8 w-auto">
                <span class="font-semibold text-slate-900 dark:text-slate-100"><%= t("app.short_name") %></span>
              <% end %>
              <div class="text-xs text-slate-500 dark:text-slate-400"><%= t("app.tagline") %></div>
            </div>
            <div class="flex items-center gap-3">
              <%= link_to t("nav.docs"), docs_path, class: "text-sm text-slate-600 hover:text-slate-900 dark:text-slate-300 dark:hover:text-white" %>
              <%= link_to t("nav.pricing"), pricing_path, class: "text-sm text-slate-600 hover:text-slate-900 dark:text-slate-300 dark:hover:text-white" %>
              <%= button_tag type: "button", class: "light-switch relative inline-flex items-center h-6 rounded-full w-11 border border-slate-300 dark:border-slate-700" do %>
                <span class="sr-only">Toggle theme</span>
                <span class="inline-block h-5 w-5 rounded-full bg-white dark:bg-slate-800 shadow transform transition"></span>
              <% end %>
              <div class="flex items-center gap-2 bg-slate-200/70 dark:bg-slate-800/80 rounded-full px-2 py-1">
                <%= link_to "EN", url_for(locale: :en), class: "text-xs px-2 py-1 rounded-full #{I18n.locale == :en ? 'bg-blue-600 text-white' : 'text-slate-600 dark:text-slate-200 hover:text-blue-600'}" %>
                <%= link_to "ES", url_for(locale: :es), class: "text-xs px-2 py-1 rounded-full #{I18n.locale == :es ? 'bg-blue-600 text-white' : 'text-slate-600 dark:text-slate-200 hover:text-blue-600'}" %>
              </div>
              <%= link_to t("nav.sign_out"), destroy_user_session_path, data: { turbo_method: :delete }, class: "btn-sm text-white bg-slate-900 hover:bg-slate-800 dark:bg-white dark:text-slate-900 dark:hover:bg-slate-200 shadow-sm" %>
            </div>
          </div>
        </header>

        <main class="flex-1 max-w-6xl mx-auto px-4 sm:px-6 py-8">
          <%= yield %>
        </main>
      </div>
    </div>

    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "mosaic/js/vendors/alpinejs.min", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "mosaic/js/main", "data-turbo-track": "reload", defer: true %>
  </body>
</html>
