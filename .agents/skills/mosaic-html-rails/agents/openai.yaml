interface:
  display_name: "Mosaic HTML Rails"
  short_description: "Port Cruip Mosaic UI into Rails with close parity."
  default_prompt: "Use $mosaic-html-rails to build or refactor a Mosaic-based dashboard, settings, support, or auth page in this Rails repo with closest practical parity to the local source."

policy:
  allow_implicit_invocation: true
